# binaries
/apiary
src/apiary
bin/
dist/

# test output
*.test
coverage.out

# env files
.env
.env.local
apiary.yaml.local

# apiary runtime state (project-scoped, written to a .apiary/ dir beside the config)
**/.apiary/apiary.db
**/.apiary/apiary.db-*
**/.apiary/apiary.sock
**/.apiary/logs/
**/.apiary/memory/
# installed plugins (binaries, fetched per host) and the state they keep
**/.apiary/plugins/
**/.apiary/routines-state.json
**/.apiary/backups/
**/.apiary/*.bak

# worktrees — single canonical location for all git worktrees
/.worktrees/

# editor
.DS_Store
.idea/
.vscode/
*.swp
.gitnexus
site/

# Built by `make registry-build` / the docs deploy from registry/plugins/*.yaml
docs/registry/
